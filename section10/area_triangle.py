def area_triangle(base ,width):
    area = base * width * 0.5
    print("Area of the triangle is: ", area)

base = float(input("Enter base value: "))
width = float(input('Enter width value: '))
area_triangle(base, width)